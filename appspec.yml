version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/myApp
hooks:
  AfterInstall:
    - location: scripts/afterinstall.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/applicationstart.sh
      timeout: 300
      runas: root
